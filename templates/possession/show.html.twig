{% extends 'base.html.twig' %}

{% block title %}Possession{% endblock %}

{% block body %}
    <h1>Possession</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ possession.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ possession.name }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ possession.valeur }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ possession.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_possession_index') }}">back to list</a>

    <a href="{{ path('app_possession_edit', {'id': possession.id}) }}">edit</a>

    {{ include('possession/_delete_form.html.twig') }}
{% endblock %}
